# Ashot

Небольшой проект для генерации рекламного вертикального видео (формат **3:4**) с помощью `ffmpeg`.

Скрипт `make_video.sh` собирает ролик длительностью 15 секунд из однотонного фона и набора текстовых/графических слоёв (`drawbox`, `drawtext`):
- заголовок `CHANCE EAU FRAICHE`;
- подпись `ЖЕНСКИЕ ДУХИ`;
- декоративные блоки;
- промо-тексты о запахе;
- отметка объёма `10ml`.

Готовый файл сохраняется в папку `output` под именем:

`output/chance_eau_fraiche_3x4.mpeg4`

## Зависимости

Для запуска нужен:
- **bash** (скрипт использует `set -euo pipefail`);
- **ffmpeg** с поддержкой фильтров `drawtext` и `drawbox`;
- шрифты DejaVu, доступные по путям:
  - `/usr/share/fonts/truetype/dejavu/DejaVuSerif-Bold.ttf`
  - `/usr/share/fonts/truetype/dejavu/DejaVuSerif.ttf`
  - `/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf`

> Если шрифтов по этим путям нет, нужно либо установить пакет DejaVu, либо поменять пути в `make_video.sh`.

## Как запустить

1. Выдать права на выполнение (один раз):
   ```bash
   chmod +x make_video.sh
   ```
2. Запустить генерацию видео:
   ```bash
   ./make_video.sh
   ```
3. После выполнения скрипт выведет путь к созданному файлу.

## Структура проекта

- `make_video.sh` — основной скрипт сборки видео.
- `README.md` — описание проекта и инструкции по запуску.
